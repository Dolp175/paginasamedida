---
// src/components/BlogCallout.astro
interface Props {
  type?: 'info' | 'warning' | 'tip' | 'error';
  children?: any;
}

const { type = 'info' } = Astro.props as Props;
const colors = {
  info: { bg: 'bg-blue-50', border: 'border-blue-200', text: 'text-blue-800', icon: 'ℹ️' },
  warning: { bg: 'bg-yellow-50', border: 'border-yellow-200', text: 'text-yellow-800', icon: '⚠️' },
  tip: { bg: 'bg-green-50', border: 'border-green-200', text: 'text-green-800', icon: '💡' },
  error: { bg: 'bg-red-50', border: 'border-red-200', text: 'text-red-800', icon: '❌' }
} as const;
---

<div class={`${colors[type].bg} ${colors[type].border} border-l-4 p-4 my-6 rounded-r-lg`}>
  <div class="flex items-start gap-3">
    <span class="text-lg mt-0.5">{colors[type].icon}</span>
    <div class={`${colors[type].text} prose`}>
      <slot />
    </div>
  </div>
</div>
